<Window x:Class="Energy_5adra_Final_Version.DevicesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Devices Voltages" Height="450" Width="650" Background="#1e1e1e"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Device list -->
        <ListView x:Name="DevicesList"
          Grid.Row="0"
          Background="#2a2a2a"
          Foreground="White"
          BorderBrush="#444"
          BorderThickness="1"
          Margin="0,0,0,10"
          FontSize="14"
          FontWeight="SemiBold">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Padding" Value="5"/>
                    <Setter Property="Margin" Value="2"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect Color="Black" BlurRadius="5" ShadowDepth="0" Opacity="0.2"/>
                        </Setter.Value>
                    </Setter>

                    <Style.Triggers>
                        <!-- Hover effect -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#3a3a3a"/>
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="#00ffff" BlurRadius="10" ShadowDepth="0" Opacity="0.6"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>

                        <!-- Selection effect -->
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#3a3a3a"/>
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="#00ffff" BlurRadius="10" ShadowDepth="0" Opacity="0.6"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.View>
                <GridView>
                    <GridViewColumn Header="🔌 Device" DisplayMemberBinding="{Binding Name}" Width="250"/>
                    <GridViewColumn Header="⚡ Voltage (V)" DisplayMemberBinding="{Binding ReferenceVoltage}" Width="150"/>
                </GridView>
            </ListView.View>
        </ListView>


        <!-- Result display -->
        <TextBlock x:Name="ResultText"
                   Grid.Row="1"
                   FontSize="16"
                   Foreground="#00ff7f"
                   FontWeight="Bold"
                   Margin="0,10,0,10"
                   TextAlignment="Center"
                   TextWrapping="Wrap"
                   Effect="{DynamicResource GlowEffect}"/>

        <!-- Button with animation -->
        <Button Grid.Row="2"
                Content="⚙️ Check Voltage"
                Width="180"
                Height="45"
                HorizontalAlignment="Center"
                Background="#00c3ff"
                Foreground="White"
                FontWeight="Bold"
                FontSize="15"
                Cursor="Hand"
                BorderThickness="0"
                Margin="0,10,0,0"
                Click="CheckVoltages_Click">
            <Button.Triggers>
                <EventTrigger RoutedEvent="Button.Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                             From="0" To="1"
                                             Duration="0:0:1.2"
                                             AutoReverse="False"/>
                            <ColorAnimation Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)"
                                            To="#00aaff"
                                            Duration="0:0:1"
                                            AutoReverse="True"
                                            RepeatBehavior="Forever"/>
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Button.Triggers>
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="10"
                                        Padding="10"
                                        SnapsToDevicePixels="True"
                                        Effect="{DynamicResource GlowEffect}">
                                    <ContentPresenter HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#0099cc"/>
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="#00ffff" BlurRadius="15" ShadowDepth="0"/>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </Grid>

    <!-- Resources -->
    <Window.Resources>
        <DropShadowEffect x:Key="GlowEffect"
                          Color="#00ffff"
                          BlurRadius="10"
                          ShadowDepth="0"
                          Opacity="0.6"/>
    </Window.Resources>
</Window>
